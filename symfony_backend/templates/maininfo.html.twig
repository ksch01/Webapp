{% extends "main.html.twig" %}

{% block body %}
    {{ info }}
    {% if link is defined %}
        <div>
        {{ linkText }} 
        <a href={{link}}>here</a>
        .
        </div>
    {% endif %}
{% endblock %}